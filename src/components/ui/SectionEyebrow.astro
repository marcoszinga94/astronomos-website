---
export interface Props {
  text: string;
}

const { text } = Astro.props;
---

<div class="inline-flex items-center gap-2 font-mono text-[12px] tracking-widest uppercase text-a1/75 mb-3.5 before:content-[''] before:w-5 before:h-px before:bg-a1/50">
  {text}
</div>
