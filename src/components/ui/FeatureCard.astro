---
export interface Props {
  icon?: string;
  title: string;
  text: string;
  iconStyle?: "emoji" | "dot";
}

const { icon = "", title, text, iconStyle = "emoji" } = Astro.props;
---

<div class="flex gap-3.5 items-start p-[18px] bg-bg3 border border-white/5 rounded-xl">
  {iconStyle === "emoji" && <span class="text-[22px] shrink-0 mt-0.5">{icon}</span>}
  {iconStyle === "dot" && <span class="w-1.5 h-1.5 rounded-full bg-a1 shrink-0 mt-1.5 shadow-[0_0_6px_#4f7fff]"></span>}
  <div>
    <div class="font-mono text-[11px] tracking-wider uppercase text-t2 mb-1">{title}</div>
    <p class="text-[15px] text-t2 leading-relaxed">{text}</p>
  </div>
</div>
